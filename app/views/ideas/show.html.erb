<div class="container"> 

  <h2><%= @idea.title %></h2>
  
  <p>
    <%= @idea.body %>
  </p>
  
  <p>
    <i>updated on <%= @idea.updated_at.to_date.to_formatted_s(:long) %></i>
  </p>

  <p>
    <%= link_to "Back to ideas", root_path %> |
    <%= link_to "Edit idea", edit_idea_path(@idea) %> |
    <%= link_to "Delete idea", idea_path(@idea), method: :delete, data: { confirm: "Are you sure you want to delete this idea?" } %>
  </p>

</div>