<div class="container"> 

  <h2>Ideas</h2>

  <ul id="ideas">
    <% @ideas.each do |idea| %>
      <li>
        <%= link_to idea.title, idea_path(idea), class: 'idea_title' %>
      </li>
    <% end %>
  </ul>
  <p>
    <%= link_to "Submit a new idea", new_idea_path, class: 'new_idea' %>
  </p>

</div>